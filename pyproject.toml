[project]
name = "wk-nhk"
requires-python = ">=3.12"
version = "0.1.0"
description = "NHK Easy News scraper with WaniKani integration"
dependencies = [
    "requests",
    "beautifulsoup4",
    "pyyaml",
    "lxml",
    "jinja2",
    "playwright",
]
